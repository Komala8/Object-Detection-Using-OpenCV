🎯 Object Detection Using OpenCV

A simple yet powerful Object Detection System built using OpenCV and Deep Learning (SSD MobileNet v3) that detects and labels multiple objects in images or video streams.
This project uses a pre-trained COCO model integrated through OpenCV’s DNN module to perform real-time object detection efficiently on CPU.

